name: OCI Instance Provisioning

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *' # 5분마다 자동 실행

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'

      - name: Install dependencies
        run: composer install --no-dev --prefer-dist --no-progress

      # 💡 [버그 3 해결] PEM 키 파일 생성 후 안전하게 권한(chmod 600)을 부여합니다.
      - name: Create private key file
        run: |
          echo "${{ secrets.OCI_KEY_CONTENT }}" > oci_private_key.pem
          chmod 600 oci_private_key.pem

      - name: Run Provisioning Script
        id: run_script
        run: |
          OUTPUT=$(php index.php)
          echo "$OUTPUT"
          
          # 1. 출력물에 확실한 에러/용량 부족 키워드가 포함되어 있는지 검사합니다.
          # 2. 동시에 출력물에 'ocid1.instance'라는 진짜 식별자가 생성되었는지 이중 체크합니다.
          if [[ "$OUTPUT" != *"Out of capacity"* ]] && [[ "$OUTPUT" != *"InternalError"* ]] && [[ "$OUTPUT" == *"ocid1.instance"* ]]; then
            echo "INSTANCE_CREATED=true" >> $GITHUB_OUTPUT
          else
            echo "INSTANCE_CREATED=false" >> $GITHUB_OUTPUT
          fi
        env:
          OCI_USER_ID: ${{ secrets.OCI_USER }}
          OCI_TENANCY_ID: ${{ secrets.OCI_TENANCY }}
          OCI_KEY_FINGERPRINT: ${{ secrets.OCI_FINGERPRINT }}
          OCI_PRIVATE_KEY_FILENAME: oci_private_key.pem
          OCI_REGION: ${{ secrets.OCI_REGION }}
          OCI_SUBNET_ID: ${{ secrets.OCI_SUBNET_ID }}
          OCI_IMAGE_ID: ${{ secrets.OCI_IMAGE_ID }}
          OCI_SHAPE: ${{ secrets.OCI_SHAPE }}
          OCI_OCPUS: ${{ secrets.OCI_OCPUS }}
          OCI_MEMORY_IN_GBS: ${{ secrets.OCI_MEMORY_IN_GBS }}
          OCI_SSH_PUBLIC_KEY: ${{ secrets.OCI_SSH_PUBLIC_KEY }}

      - name: Send Telegram Notification
        # 이전 step에서 최종 판단한 결과가 정확히 'true'일 때만 실행합니다.
        if: steps.run_script.outputs.INSTANCE_CREATED == 'true'
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          curl -s -X POST https://api.telegram.org/bot$TELEGRAM_TOKEN/sendMessage \
            -d chat_id=$TELEGRAM_CHAT_ID \
            -d text="🎉 [오라클 클라우드] ARM 인스턴스가 진짜로 생성되었습니다! 콘솔을 확인하세요."
